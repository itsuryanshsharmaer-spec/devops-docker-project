version: "3"

services:
  frontend:
    build: ./frontend
    ports:
      - "8666:80"

  backend:
    build: ./backend
    ports:
      - "5000:5000"

  mongo:
    image: mongo
    volumes:
      - mongo_data:/data/db   
    ports:
      - "27017:27017"
volumes:
  mongo_data: 
